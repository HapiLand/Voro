namespace VoroSystem.VoroDataStructures.EffectDefinition {
class Foo {
  static void Main() {
    /*var slope = EffecttFactory.Create(EffectVariants.Slope);
    Console.WriteLine(slope.EffectType);
    foreach (var param in slope.Parameters) {
      Console.WriteLine($"- {param.Name} ({param.ParameterVariant}): {param.Value}");
    }

    var noise = EffecttFactory.Create(EffectVariants.Noise);
    Console.WriteLine(noise.EffectType);
    foreach (var param in noise.Parameters) {
      Console.WriteLine($"- {param.Name} ({param.ParameterVariant}): {param.Value}");
    }*/
  }
}
}