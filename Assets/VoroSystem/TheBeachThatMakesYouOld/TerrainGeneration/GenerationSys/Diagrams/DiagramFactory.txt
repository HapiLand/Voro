using System.Collections.Generic;
using VoroSystem.EditorGraph.GraphSys.Graphs;
using VoroSystem.WorldGrid;

namespace VoroSystem.TerrainGeneration.GenerationSys.Diagrams {
public static class DiagramFactory {
    public static Diagram CreateDiagram(List<Graph> graphs, IWorld worldMapSystem) {
        return new Diagram(graphs, worldMapSystem);
    }
}
}