using VoroSystem.Terrain.Generation.PostCompute;
using VoroSystem.Terrain.Generation.PreCompute;
using VoroSystem.WorldGrid.Grids;

namespace VoroSystem.Terrain.Generation.Compute {
public interface IGenerator {
    /*/// <summary> generate the full World Terrain data from the provided Diagram </summary>
    IResult GenerateComputedResultForFullWorld(IDiagram diagram);

    /// <summary> generate partial World Terrain data for a single Tile </summary>
    IResult GeneratePartialComputedResultForATile(IDiagram diagram, ITile tile);

    /// <summary> determine whether the given Tile requires generating </summary>
    bool DoesTileNeedGenerating(ITile tile);*/
}
}