using UnityEngine;

namespace VoroSystem.Terrain.World {
public class WorldObject : IWorldObject {
    /*public WorldObject(Vector3 worldPosition, int id) {
        WorldPosition = worldPosition;
        ID = id;
        MeshPieceAsset = AssetLoader.LoadMeshPiece(ID);
    }

    public Vector3 WorldPosition { get; }
    public int ID { get; }
    public Mesh MeshPieceAsset { get; }*/
}
}