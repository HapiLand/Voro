namespace Terrain {
/// <summary>
/// an middle man between Voro and the UnityEngine
/// so that Geometry data can be turned into a format
/// which is suitable for the engine to Instantiate
/// </summary>
internal class GeometryBuilder { }
}